package StuProject;

import java.util.Comparator;

public class StuTotal implements Comparator<StuInfo> {

	@Override
	public int compare(StuInfo o1, StuInfo o2) {

		if (o1.total < o2.total) {
			return -1;
		} else if (o1.total > o2.total) {
			return 1;
		}
		return 0;
		
	}

}
